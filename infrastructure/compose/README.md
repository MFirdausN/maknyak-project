# Local infrastructure

The canonical Compose entrypoint lives at the repository root so `docker compose` works without flags. This directory is reserved for component configuration, initialization, and future observability files.
